# sigcorp

Conexão com o banco de dados Mysql. Alterar no arquivo appsettings.json a string de conexão com o banco

"ConnectionStrings": {
    "SigcorpContext": "server=localhost;userid=SeuUsuario;password=SeuPassword;database=sigcorptestedb"
  }